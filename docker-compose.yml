version: '3.8'

services:
  ban4life:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ban4life-bot
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data

