diff --git a/lib/Socket/messages-send.js b/lib/Socket/messages-send.js
index b1e25574c290b747175db0fcc36977098cdfd4af..792d7095c9fc24ae3df42708db870cfba7b54387 100644
--- a/lib/Socket/messages-send.js
+++ b/lib/Socket/messages-send.js
@@ -281,8 +281,8 @@ const makeMessagesSocket = (config) => {
             if (!isGroup && !isStatus) {
                 additionalAttributes = { ...additionalAttributes, 'device_fanout': 'false' };
             }
-            const { user, device } = (0, WABinary_1.jidDecode)(participant.jid);
-            devices.push({ user, device });
+            const { user, device, server: partServer } = (0, WABinary_1.jidDecode)(participant.jid);
+            devices.push({ user, device, server: partServer });
         }
         await authState.keys.transaction(async () => {
             var _a, _b, _c, _d, _e;
@@ -320,8 +320,15 @@ const makeMessagesSocket = (config) => {
                     }
                     const additionalDevices = await getUSyncDevices(participantsList, !!useUserDevicesCache, false);
                     devices.push(...additionalDevices);
+                    // Ensure every participant has at least device 0 if USync omitted them
+                    for (const pJid of participantsList) {
+                        const decoded = (0, WABinary_1.jidDecode)(pJid);
+                        if (decoded && !devices.some(d => d.user === decoded.user)) {
+                            devices.push({ user: decoded.user, device: 0, server: decoded.server });
+                        }
+                    }
                 }
-                const patched = await patchMessageBeforeSending(message, devices.map(d => (0, WABinary_1.jidEncode)(d.user, isLid ? 'lid' : 's.whatsapp.net', d.device)));
+                const patched = await patchMessageBeforeSending(message, devices.map(d => (0, WABinary_1.jidEncode)(d.user, d.server || (isLid ? 'lid' : 's.whatsapp.net'), d.device)));
                 const bytes = (0, Utils_1.encodeWAMessage)(patched);
                 const { ciphertext, senderKeyDistributionMessage } = await signalRepository.encryptGroupMessage({
                     group: destinationJid,
@@ -330,8 +337,8 @@ const makeMessagesSocket = (config) => {
                 });
                 const senderKeyJids = [];
                 // ensure a connection is established with every device
-                for (const { user, device } of devices) {
-                    const jid = (0, WABinary_1.jidEncode)(user, isLid ? 'lid' : 's.whatsapp.net', device);
+                for (const { user, device, server: devServer } of devices) {
+                    const jid = (0, WABinary_1.jidEncode)(user, devServer || (isLid ? 'lid' : 's.whatsapp.net'), device);
                     if (!senderKeyMap[jid] || !!participant) {
                         senderKeyJids.push(jid);
                         // store that this person has had the sender keys sent to them
@@ -363,9 +370,11 @@ const makeMessagesSocket = (config) => {
             else {
                 const { user: meUser } = (0, WABinary_1.jidDecode)(meId);
                 if (!participant) {
-                    devices.push({ user });
+                    const { server: userServer } = (0, WABinary_1.jidDecode)(jid) || {};
+                    devices.push({ user, server: userServer });
                     if (user !== meUser) {
-                        devices.push({ user: meUser });
+                        const { server: meServer } = (0, WABinary_1.jidDecode)(meId) || {};
+                        devices.push({ user: meUser, server: meServer });
                     }
                     if ((additionalAttributes === null || additionalAttributes === void 0 ? void 0 : additionalAttributes['category']) !== 'peer') {
                         const additionalDevices = await getUSyncDevices([meId, jid], !!useUserDevicesCache, true);
@@ -375,9 +384,9 @@ const makeMessagesSocket = (config) => {
                 const allJids = [];
                 const meJids = [];
                 const otherJids = [];
-                for (const { user, device } of devices) {
+                for (const { user, device, server: devServer } of devices) {
                     const isMe = user === meUser;
-                    const jid = (0, WABinary_1.jidEncode)(isMe && isLid ? ((_c = (_b = authState.creds) === null || _b === void 0 ? void 0 : _b.me) === null || _c === void 0 ? void 0 : _c.lid.split(':')[0]) || user : user, isLid ? 'lid' : 's.whatsapp.net', device);
+                    const jid = (0, WABinary_1.jidEncode)(isMe && isLid ? ((_c = (_b = authState.creds) === null || _b === void 0 ? void 0 : _b.me) === null || _c === void 0 ? void 0 : _c.lid.split(':')[0]) || user : user, devServer || (isLid ? 'lid' : 's.whatsapp.net'), device);
                     if (isMe) {
                         meJids.push(jid);
                     }
diff --git a/lib/Utils/signal.js b/lib/Utils/signal.js
index 745f91974f0f917e2f7698a7f05eb50cdcb4e8f9..65d7499f37e8e0a42aeb31e52767d87f7a48eae6 100644
--- a/lib/Utils/signal.js
+++ b/lib/Utils/signal.js
@@ -99,7 +99,7 @@ const extractDeviceJids = (result, myJid, excludeZeroDevices) => {
     const extracted = [];
     for (const userResult of result) {
         const { devices, id } = userResult;
-        const { user } = (0, WABinary_1.jidDecode)(id);
+        const { user, server } = (0, WABinary_1.jidDecode)(id);
         const deviceList = devices === null || devices === void 0 ? void 0 : devices.deviceList;
         if (Array.isArray(deviceList)) {
             for (const { id: device, keyIndex } of deviceList) {
@@ -107,7 +107,7 @@ const extractDeviceJids = (result, myJid, excludeZeroDevices) => {
                     (myUser !== user || myDevice !== device) && // either different user or if me user, not this device
                     (device === 0 || !!keyIndex) // ensure that "key-index" is specified for "non-zero" devices, produces a bad req otherwise
                 ) {
-                    extracted.push({ user, device });
+                    extracted.push({ user, device, server });
                 }
             }
         }
diff --git a/lib/WABinary/jid-utils.d.ts b/lib/WABinary/jid-utils.d.ts
index 4068367f346ec490a7c02b8ff37ebe10bbf94b80..844900e5d9d93480b9de7399ea01a93e610d95a6 100644
--- a/lib/WABinary/jid-utils.d.ts
+++ b/lib/WABinary/jid-utils.d.ts
@@ -7,6 +7,7 @@ export type JidServer = 'c.us' | 'g.us' | 'broadcast' | 's.whatsapp.net' | 'call
 export type JidWithDevice = {
     user: string;
     device?: number;
+    server?: JidServer;
 };
 export type FullJid = JidWithDevice & {
     server: JidServer;
